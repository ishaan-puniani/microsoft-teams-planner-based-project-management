import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const requirementImporterSelectors = importerSelectors(
  'requirement.importer',
);

export default requirementImporterSelectors;
