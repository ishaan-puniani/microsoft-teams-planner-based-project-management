import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const testCaseImporterSelectors = importerSelectors(
  'testCase.importer',
);

export default testCaseImporterSelectors;
