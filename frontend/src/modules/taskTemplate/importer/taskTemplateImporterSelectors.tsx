import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const taskTemplateImporterSelectors = importerSelectors(
  'taskTemplate.importer',
);

export default taskTemplateImporterSelectors;
